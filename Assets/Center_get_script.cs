using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Center_get_script : MonoBehaviour
{
    public static Center_get_script center_script;
    // Start is called before the first frame update
    void Start()
    {
        
    }
    private void Awake()
    {
        center_script = this;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
