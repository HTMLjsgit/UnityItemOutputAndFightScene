using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerTextGetScript : MonoBehaviour
{
    public static PowerTextGetScript power_text;
    // Start is called before the first frame update
    void Start()
    {
    }
    private void Awake()
    {
        power_text = this;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
