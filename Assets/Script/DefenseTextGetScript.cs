using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DefenseTextGetScript : MonoBehaviour
{
    public static DefenseTextGetScript defense_text;
    // Start is called before the first frame update
    void Start()
    {
        
    }
    private void Awake()
    {
        defense_text = this;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
